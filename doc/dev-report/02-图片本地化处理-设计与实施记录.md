# 图片本地化处理 — 设计与实施记录

> 状态：完成
> 需求（2026-09-13）：扫描站点全部 Markdown 中的远程图片链接，逐一下载到本地，并把文档链接改写为本地相对路径，消除对第三方图床的运行时依赖。

---

## 一、背景与目标

JavaGuide 文档中存在大量引用自 `oss.javaguide.cn`、`cdn.jsdelivr.net` 等外链图床的图片。为降低对外部服务可用性的依赖、提升本地构建/离线浏览可靠性，将可达的远程图片全部下载到本地并改写链接。

**目标**：把可下载的远程图片镜像到 `.vuepress/public/assets/images/<host>/<path>` 目录，并将 `docs/**/*.md` 中的远程链接改写为 `/assets/images/...` 本地绝对路径；对无法下载的链接保持回退为原远程 URL。

---

## 二、实施方案

| 步骤 | 脚本                                      | 说明                                                                                                                                     |
| ---- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 扫描 | `scripts/scan-remote-images.mjs`          | 遍历 `docs/**/*.md`，用正则抽取 Markdown 图片 `![](...)` 与 HTML `<img src>` 中的远程 URL，去重生成清单 `scripts/tmp/remote-images.json` |
| 下载 | `scripts/download-remote-images.mjs`      | 按 host 镜像目录逐张下载，忽略 query/非法字符，并发下载，记录 `scripts/tmp/download-report.json`（success/failed）                       |
| 改写 | `scripts/rewrite-remote-image-links.mjs`  | 将成功下载的远程链接替换为 `/assets/images/<镜像相对路径>`，记录 `scripts/tmp/rewrite-report.json`                                       |
| 校验 | `scripts/verify-local-images.mjs`         | 校验文档中所有 `/assets/images/...` 引用对应真实文件是否存在                                                                             |
| 回滚 | `scripts/restore-failed-remote-links.mjs` | 将下载失败的图片本地路径改回原远程 URL                                                                                                   |

**镜像路径规则**：`/assets/images/<host>/<pathname 逐段去除非法字符>`，域名与目录层级一一对应，便于追溯来源。

**路径规范化**：对 URL `pathname` 进行 `decodeURIComponent`，剥离空段与 `.` 段，对每一段做非法字符清洗（空格、`&`、`?`、中文保留），确保 Windows 文件系统合法。

---

## 三、执行结果

- **扫描**：局部化了 **2392** 处唯一图片引用。
- **下载**：共 **1957** 个唯一远程 URL，成功 **1952**，失败 **5**。
- **改写**：成功镜像的远程链接已全部改写为 `/assets/images/...` 本地路径写入 `docs/**/*.md`。
- **回滚**：5 个下载失败项全部改回原远程 URL（均为 403/鉴权/过期失效链接）。
- **校验**：文档中 `/assets/images/...` 本地引用 **0** 处缺失，全部有效。

### 下载失败并回滚为远程的 5 项

| 远程 URL                                                                                     | 所在文件                                                             |
| -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| `http://www.mybank.com/Transfer?bankId=11&money=10000`                                       | `docs/system-design/security/advantages-and-disadvantages-of-jwt.md` |
| `https://cdn.jsdelivr.net/gh/javaguide-tech/blog-images-6@main/12-04-1/分布式事务示意图.png` | `docs/roadmap/java-roadmap.md`                                       |
| `https://oss.javaguide.cn/github/dootask/image-20211226144607457.png`                        | `docs/tools/git/github-tips.md`                                      |
| `https://oss.javaguide.cn/github/cosy/image-20211227093215005.png`                           | `docs/tools/git/github-tips.md`                                      |
| `https://oss.javaguide.cn/github/dootask/repobeats.png`                                      | `docs/tools/git/github-tips.md`                                      |

> 注：`docs/about-the-author/javaguide-100k-star.md` 中另有 1 处带过期签名的图片（`e=1643644799&token=...`）原本即失效，无法下载，保持原远程链接不变，属既有失效链接。

---

## 四、遗留与说明

- 脚本位于 `scripts/`，中间产物在 `scripts/tmp/`（`remote-images.json`、`download-report.json`、`rewrite-report.json`）。临时脚本 `scripts/tmp/check-remaining-remote.mjs` 用于核查改写后剩余的远程引用。
- 5 个回滚项为源文档中本就失效的链接，保持远程形式以保留引用语义。
- 本地图片位于 `.vuepress/public/assets/images/`，随静态资源一同打包，无需额外配置。
